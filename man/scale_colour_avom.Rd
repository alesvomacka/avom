% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/color-palettes.R
\name{scale_fill_avom}
\alias{scale_fill_avom}
\alias{scale_colour_avom}
\alias{scale_color_avom}
\title{avom discrete color and fill scales for ggplot2}
\usage{
scale_fill_avom(palette = "catpuccin_latte", direction = 1, ...)

scale_colour_avom(palette = "catpuccin_latte", direction = 1, ...)

scale_color_avom(palette = "catpuccin_latte", direction = 1, ...)
}
\arguments{
\item{palette}{Name of the palette. See \code{\link[=avom_palette]{avom_palette()}} for the full list of valid names.
Defaults to \code{"catpuccin_latte"}.}

\item{direction}{\code{1} (default) uses colors in their natural order; \code{-1} reverses the order.}

\item{...}{Additional arguments passed to \code{\link[ggplot2:discrete_scale]{ggplot2::discrete_scale()}}.}
}
\value{
A ggplot2 \code{Scale} object to be added to a plot with \code{+}.
}
\description{
Discrete ggplot2 scales that apply an avom color palette to the \code{colour} or \code{fill} aesthetic.
Three aliases are provided:
\itemize{
\item \code{scale_colour_avom()} — maps to the \code{colour} aesthetic (British spelling).
\item \code{scale_color_avom()} — alias for \code{scale_colour_avom()} (American spelling).
\item \code{scale_fill_avom()} — maps to the \code{fill} aesthetic.
}

All functions delegate to \code{\link[ggplot2:discrete_scale]{ggplot2::discrete_scale()}} via \code{\link[=palette_gen]{palette_gen()}}, so any argument
accepted by \code{discrete_scale()} (e.g. \code{name}, \code{breaks}, \code{labels}, \code{na.value}, \code{drop}) can
be passed through \code{...}.
}
\examples{
ggplot2::ggplot(data = mtcars,
                mapping = ggplot2::aes(x = mpg, y = hp, color = as.factor(cyl))) +
  ggplot2::geom_point() +
  scale_colour_avom("catpuccin_latte")

# Fill aesthetic
ggplot2::ggplot(data = mtcars,
                mapping = ggplot2::aes(x = as.factor(cyl), fill = as.factor(cyl))) +
  ggplot2::geom_bar() +
  scale_fill_avom("gruvbox", direction = -1)
}
\seealso{
\code{\link[=avom_palette]{avom_palette()}}, \code{\link[=palette_gen]{palette_gen()}}, \code{\link[ggplot2:discrete_scale]{ggplot2::discrete_scale()}}
}
